name: 测试调用 release-like

on:
  workflow_dispatch:

jobs:
  test-like:
    uses: ./.github/workflows/test-release-like.yaml
    with:
      device-id: shiba
      magisk-preinit-device: sda10
    secrets: inherit
